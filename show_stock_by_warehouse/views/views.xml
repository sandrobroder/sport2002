<odoo>
    <data>
        <record id="view_stock_product_tree" model="ir.ui.view">
            <field name="name">product.stock.tree.inherit</field>
            <field name="model">product.product</field>
            <field name="inherit_id" ref="product.product_product_tree_view"/>
            <field name="arch" type="xml">
                <field name="price" position="after">
                    <field name="on_hand_qty_wh_one" attrs="{'invisible':[('type', '!=', 'product')] }" optional="show"
                           decoration-bf="1" />
                    <field name="allowed_warehouse_one_id" string="Warehouse One" invisible="1"/>
                    <field name="on_hand_qty_wh_two" attrs="{'invisible':[('type', '!=', 'product')]}" decoration-bf="1"
                           string="WH Two On-Hand Qty" optional="show"/>
                    <field name="allowed_warehouse_two_id" string="Warehouse Two" invisible="1"/>
                </field>
            </field>
        </record>

        <record id="view_stock_product_template_tree" model="ir.ui.view">
            <field name="name">product.template.stock.tree.inherit</field>
            <field name="model">product.template</field>
            <field name="inherit_id" ref="product.product_template_tree_view"/>
            <field name="arch" type="xml">
                <field name="standard_price" position="after">
                    <field name="on_hand_qty_wh_one" attrs="{'invisible':[('type', '!=', 'product')] }" optional="show"
                           decoration-bf="1" />
                    <field name="allowed_warehouse_one_id" string="Warehouse One" invisible="1"/>
                    <field name="on_hand_qty_wh_two" attrs="{'invisible':[('type', '!=', 'product')]}" decoration-bf="1"
                            optional="show"/>
                    <field name="allowed_warehouse_two_id" string="Warehouse Two" invisible="1"/>
                </field>
            </field>
        </record>

        <record model="ir.ui.view" id="stock.product_template_kanban_stock_view">
            <field name="name">Product Template Kanban Stock</field>
            <field name="model">product.template</field>
            <field name="inherit_id" ref="product.product_template_kanban_view"/>
            <field name="arch" type="xml">
                <xpath expr="//kanban" position="inside">
                    <field name="type"/>
                    <field name="allowed_warehouse_one_id"/>
                    <field name="allowed_warehouse_two_id"/>
                </xpath>
                <xpath expr="//div[@name='product_lst_price']" position="after">
                    <div t-if="record.type.raw_value == 'product' and record.allowed_warehouse_one_id.raw_value"><field name="allowed_warehouse_one_id"/> On hand:
                        <field name="on_hand_qty_wh_one"/>
                        <field name="uom_id"/>
                    </div>

                    <div t-if="record.type.raw_value == 'product' and record.allowed_warehouse_two_id.raw_value"><field name="allowed_warehouse_two_id"/> On hand:
                        <field name="on_hand_qty_wh_two"/>
                        <field name="uom_id"/>
                    </div>
                </xpath>
            </field>
        </record>


        <record id="view_res_users_form_inherit" model="ir.ui.view">
            <field name="name">res.users.form.inherit</field>
            <field name="model">res.users</field>
            <field name="inherit_id" ref="base.view_users_form"/>
            <field name="arch" type="xml">
                <field name="tz_offset" position="after">
                    <field name="warehouse_id"/>
                </field>
            </field>
        </record>

        <!-- explicit list view definition -->
        <!--
            <record model="ir.ui.view" id="show_stock_by_warehouse.list">
              <field name="name">show_stock_by_warehouse list</field>
              <field name="model">show_stock_by_warehouse.show_stock_by_warehouse</field>
              <field name="arch" type="xml">
                <tree>
                  <field name="name"/>
                  <field name="value"/>
                  <field name="value2"/>
                </tree>
              </field>
            </record>
        -->

        <!-- actions opening views on models -->
        <!--
            <record model="ir.actions.act_window" id="show_stock_by_warehouse.action_window">
              <field name="name">show_stock_by_warehouse window</field>
              <field name="res_model">show_stock_by_warehouse.show_stock_by_warehouse</field>
              <field name="view_mode">tree,form</field>
            </record>
        -->

        <!-- server action to the one above -->
        <!--
            <record model="ir.actions.server" id="show_stock_by_warehouse.action_server">
              <field name="name">show_stock_by_warehouse server</field>
              <field name="model_id" ref="model_show_stock_by_warehouse_show_stock_by_warehouse"/>
              <field name="state">code</field>
              <field name="code">
                action = {
                  "type": "ir.actions.act_window",
                  "view_mode": "tree,form",
                  "res_model": model._name,
                }
              </field>
            </record>
        -->

        <!-- Top menu item -->
        <!--
            <menuitem name="show_stock_by_warehouse" id="show_stock_by_warehouse.menu_root"/>
        -->
        <!-- menu categories -->
        <!--
            <menuitem name="Menu 1" id="show_stock_by_warehouse.menu_1" parent="show_stock_by_warehouse.menu_root"/>
            <menuitem name="Menu 2" id="show_stock_by_warehouse.menu_2" parent="show_stock_by_warehouse.menu_root"/>
        -->
        <!-- actions -->
        <!--
            <menuitem name="List" id="show_stock_by_warehouse.menu_1_list" parent="show_stock_by_warehouse.menu_1"
                      action="show_stock_by_warehouse.action_window"/>
            <menuitem name="Server to list" id="show_stock_by_warehouse" parent="show_stock_by_warehouse.menu_2"
                      action="show_stock_by_warehouse.action_server"/>
        -->
    </data>
</odoo>